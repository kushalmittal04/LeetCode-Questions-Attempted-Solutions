#include<bits/stdc++.h>

class Solution {
    public:
        int numberOfSubstrings(string s) {
            int counts = 0;
            int len = s.length();
    
            for(int i=0 ; i<n ; i++) {
                int 
            }
        }
    };